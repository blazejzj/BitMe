-   Add function to delete an user (with confirmation consisting of their password)
    -   Should delete all groups they own + messages

~~ - Add function to delete a group user owns (only if they own it) ~~
