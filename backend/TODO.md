1. Add function to delete an user (with confirmation consisting of their password)
   1.1 Should delete all groups they own + messages
   ~~ 2. Add function to delete a group user owns (only if they own it) ~~
2. Add possiblity to invite people to group (ADMIN)
3. Add possibility to remove people from group (ADMIN)
4. Add possibility to change groupname
   (ADMIN)
5. Add function to accept a group invitation
6. Add a function to reject a group invitation
7. Add a function which gathers all groups an user is a part of
8. Add a function which gathers all invitations user has
9. Add a function allowing user to leave group
10. Add a function allowing to transfer admin role to another user
11. Add a function fetching a specific group based on ID
